let a = random::integer (0, 100); # one of 0-99
#emit a;

let b = origin::host();

let c = 2 + 2;
let d = 2 xor 3;

##record::  # hell(

#random::

random::string(length);

string::format(format, arg, …);

array[chash::jump()]

chash::jump()

match event of

emit

##emit 42 >> 63;
