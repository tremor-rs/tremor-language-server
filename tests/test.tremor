let a = random::integer (0, 100); # one of 0-99
#emit a;

let b = origin::host();

let c = 2 + 2;
let d = 2 xor 3;

#record::  # hell(

#random::

random::string(length);

string::format(format, arg, …);

array[chash::jump()]

hello chash::jump()

`chash::jump()`

"chash::jump"

"random string {random::string()}."

event.`random::string()`

ddd  random:: aa

match event of

emit

##emit 42 >> 63;
